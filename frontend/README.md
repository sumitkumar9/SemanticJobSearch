# RoleMatch frontend

This is the React interface for the RoleMatch semantic job search app, built with Vite.

```bash
npm install
npm run dev
```

Vite opens the app at http://localhost:5173. Start the backend and Qdrant first. To change the backend URL, copy `.env.example` to `.env` and set `VITE_BACKEND_URL`.
